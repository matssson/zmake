For Windows, download the latest SDL at https://www.libsdl.org/download-2.0.php
by going to Development Libraries and choosing the Visual C++ 32/64-bit version.

Then put the header files in /include, the libraries in /lib/SDL2 and SDL2.dll in /target.